<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.seu.dao.SuccessKilledDAO">
    <insert id="insertSuccessKilled">
        INSERT ignore INTO success_seckill (seckill_id,user_phone) VALUES (#{successkilledId},#{userPhone})
--         如果主键重复，应该忽略当前语句，并返回0.
    </insert>
    SuccessKilled queryByIdWithSeckill(long successkilledId);
    <select id="queryByIdWithSeckill">
--         根据id查询successKill并携带seckill实体,怎样映射到Mybatis
        SELECT sk.seckill_id,sk.user_phone,sk.create_time,sk.state,s.seckill_id "seckill.seckill_id",s.name "seckill.name",s.number "seckill.number",s.start_time "seckill.start_time",s.end_time "seckill.end_time",s.create_time "seckill.create_time"
        FROM success_seckill ak INNER JOIN sec_kill s ON sk.seckill_id=s.seckill_id WHERE sk.seckill_id=#{successkilledId}
    </select>

</mapper>